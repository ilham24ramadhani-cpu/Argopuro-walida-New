# Production (Railway): hanya yang dipakai app.py + gunicorn.
# Fiona/geopandas/shapely membutuhkan GDAL sistem — tidak dipakai di Flask app ini.

Flask==3.0.3
Werkzeug==3.0.2
flask-cors==6.0.2
gunicorn==23.0.0
pymongo==3.11.0
python-dotenv==1.0.1
PyJWT==2.8.0
